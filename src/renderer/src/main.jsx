import React from "react";
import { createRoot } from "react-dom/client";

import "./styles.css";
import FloatingRoutineManager from "./App";

createRoot(document.getElementById("root")).render(<FloatingRoutineManager />);
